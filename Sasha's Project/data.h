#include "global.h"

/*encodes the numbers after the command in the data image */
int run_data(char *,int *,machine_code *,int ,int *);

/* encodes the string from the input into the data image */
void run_string(Line *,int *,machine_code *,int *);